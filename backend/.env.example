# Banco de dados (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/site_vendas_oleo?schema=public"

# Opcional: banco separado usado pelos testes de integração (npm run test -w backend).
# Se ausente, os testes usam o mesmo DATABASE_URL acima (não recomendado em produção).
TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/site_vendas_oleo_test?schema=public"

# Servidor
PORT=3333
NODE_ENV=development

# CORS — origem do frontend admin
CORS_ORIGIN="http://localhost:5173"

# Autenticação
JWT_SECRET="troque-por-um-segredo-forte-em-producao"
JWT_EXPIRES_IN="8h"

# Cloudflare R2 (compatível com S3)
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME="site-vendas-oleo"
# Endpoint S3 do R2: https://<ACCOUNT_ID>.r2.cloudflarestorage.com
R2_ENDPOINT="https://<ACCOUNT_ID>.r2.cloudflarestorage.com"
# URL pública do bucket (domínio customizado ou r2.dev), usada para montar as URLs salvas em ProductImage.url
R2_PUBLIC_URL="https://pub-xxxxxxxx.r2.dev"

# Anthropic (Claude) — interpreta instruções de edição de foto sob demanda
# no painel admin. Nunca é chamado automaticamente.
ANTHROPIC_API_KEY=""
